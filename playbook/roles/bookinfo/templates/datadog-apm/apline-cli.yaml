apiVersion: v1
kind: Pod
metadata:
  name: alpine
  namespace: default
spec:
  containers:
  - image: alpine:3.13
    command:
      - /bin/sh
      - "-c"
      - "sleep 600m"
    imagePullPolicy: IfNotPresent
    name: alpine
  restartPolicy: Always
